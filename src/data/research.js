// No published research yet. Populate this array as real papers, technical
// reports or prototype write-ups become available — the Research page is
// built to handle both the empty state and a populated list.
//
// Shape: { slug, category: 'Publication' | 'Prototype' | 'Technical Report',
//          title, summary, date: 'YYYY-MM-DD', link }

const research = [];

export const categories = ['Publication', 'Prototype', 'Technical Report'];

export default research;
